package integers

// Add takes two integers and returns the sum of them. (this comment will appear in the Go Doc just like when you look at the standard library's documentation.)
// To try this out, run godoc -http=:6060 and navigate to http://localhost:6060/pkg/integers
func Add(x, y int) int {
	return x + y
}
